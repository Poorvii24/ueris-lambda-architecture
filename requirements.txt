# Core API
flask>=3.0.0
flask-cors>=4.0.0
gunicorn>=21.2.0

# Database
pymongo>=4.6.0
dnspython>=2.6.0

# Live data fetching (background worker)
requests>=2.31.0

# ML (for future Isolation Forest phase)
scikit-learn>=1.4.0
numpy>=1.26.0

# Note: pyspark and confluent-kafka are LOCAL ONLY tools
# They are NOT installed on Render — batch layer runs locally