__pycache__/
*.pyc
*.pyo
*.pyd
.env
.env.*
*credentials*
venv/
.venv/

# Storage layer (common/storage.py) -- folders are tracked via .gitkeep,
# their contents are not. Trained model files, archives, and logs can be
# large/numerous and are regenerated by running the batch layer, not
# something to version control.
models/*
!models/.gitkeep
archives/*
!archives/.gitkeep
processed/*
!processed/.gitkeep
cache/*
!cache/.gitkeep
logs/*
!logs/.gitkeep

data/checkpoint/
data/streaming_input/

.idea/
.vscode/

*.log

.DS_Store
Thumbs.db

node_modules/

*.csv