"""
ai_layer/
=========
UERIS — Enterprise AI & Advanced Analytics Layer

Modules:
    feature_engineering   — rolling averages, lag features, THI, pollution trend rate
    forecasting           — Random Forest, XGBoost, LightGBM multi-horizon forecasting
    anomaly_ensemble      — Isolation Forest + LOF + One-Class SVM ensemble
    explainability        — SHAP values, feature importance, natural language explanations
    trend_intelligence    — seasonal decomposition, year-over-year, correlation ranking
    insight_generator     — automated executive summary generation
    model_registry        — model persistence, versioning, loading
"""
