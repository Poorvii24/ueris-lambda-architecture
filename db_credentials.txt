*** SECURITY NOTICE ***
This file previously contained a live Atlas username/password in plain
text and was committed to git history. Treat that password as
compromised -- rotate it in Atlas (Database Access > Edit User).

Atlas will ask for DB credentials when you create a database user.
Store the real values in a password manager or your local .env only,
never in a file that gets committed.

Example (placeholder, not a real credential):
Username: <your-db-username>
Password: <your-db-password>
mongodb+srv://<username>:<password>@<cluster-host>/?appName=Cluster0
